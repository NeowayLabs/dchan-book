language: go

go:
  - 1.4
  - tip
install:
  - cd $HOME/gopath/src/github.com/NeowayLabs/dchan
  - make tangle
  - go get -d -v ./unix/dchan/...
  - make build
script:
  - go get golang.org/x/tools/cmd/vet
  - go get github.com/axw/gocov/gocov
  - go get github.com/mattn/goveralls
  - go get -u github.com/golang/lint/golint
  - go get github.com/nielsdraaisma/godep
  - if ! go get code.google.com/p/go.tools/cmd/cover; then go get golang.org/x/tools/cmd/cover; fi
  - cd unix/dchan && go test -v ./...
#  - goveralls -coverprofile=coverage.txt -service=travis-ci
before_install:
  - sudo rm /etc/dpkg/dpkg.cfg.d/multiarch
  - curl https://repogen.simplylinux.ch/txt/sources_52e6d80fad9c7b2d617f3c30b5fef19e6b154d32.txt | sudo tee /etc/apt/sources.list
  - sudo apt-get update -qq
  - sudo apt-get install --force-yes emacs24
  - sudo pip install codecov
  - cd $HOME && git clone https://github.com/tiago4orion/Emacs.d.git .emacs.d
  - cd /tmp && git clone https://github.com/fniessen/orgmk
  - cd /tmp/orgmk && ORGMK_EL=$HOME/.emacs.d/init.el make -e && sudo make install
after_success:
  codecov
notifications:
  email:
    - tiago.natel@neoway.com.br
